apply plugin: 'java-library'

ext {
    mapVinaArtifactId = 'android-sdk-geojson'
}

dependencies {
  // Gson
  api libs.gson

  // Annotations
  compileOnly libs.supportAnnotation

  // Test Dependencies
  testImplementation libs.okhttp3Mockwebserver
}
